server:
  port: 8080

spring:
  application:
    name: auto-ui-platform
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

logging:
  level:
    com.autoui.platform: DEBUG